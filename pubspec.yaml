name: flutter_movie_db
description: Belajar membuat aplikasi flutter movie db

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_native_splash: ^2.2.16
  dio: ^4.0.6
  provider: ^6.0.4
  dartz: ^0.10.1
  carousel_slider: ^4.1.1
  infinite_scroll_pagination: ^3.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
